// binary search tree

console.log("..........binary tree search.............");
console.log("..........binary tree search.............");
class Node {
  constructor(data, left, right, show) {
    this.data = data;
    this.left = left;
    this.right = right;
    this.show = show;
  }
}
function insert(data) {
  let n = new Node(data, null, null);

  debugger;
  if (this.root === null) {
    console.log("this.root1:", this.root);
    this.root = n;
    console.log("this.root2:", this.root);
  } else {
    let current = this.root;
    console.log(current);
  }
}

console.log(insert(1));
